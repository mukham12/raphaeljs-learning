let paper = new Raphael('canvas-container', window.innerWidth, window.innerHeight)

// Learning easing functions for animation
let square = paper.rect(500, 100, 50, 50).attr({ 'fill': '#09c', cursor: 'pointer' })
